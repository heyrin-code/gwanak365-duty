name: 업무 알람

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 업무 알람 체크
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: node tasks-notify.js
